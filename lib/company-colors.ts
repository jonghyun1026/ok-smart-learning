/**
 * 업체 비교(레이더 차트, AI 의사결정 대시보드 등)에 쓰는 categorical 팔레트.
 *
 * 2026-07-22 재검증: 이전 팔레트는 슬롯 1을 OK 브랜드 오렌지(#F04E23)로 고정했는데, 이 화면은
 * 여러 업체를 "동등하게" 비교하는 자리라 특정 업체가 우연히 슬롯 1을 배정받으면 그 업체만
 * 앱의 브랜드 색(버튼/배지/탭 강조와 동일)을 쓰는 것처럼 보여 편들기로 오해될 수 있다. 또한
 * 두 번째 슬롯이 파란색이라 첫 업체(빨강 계열)와 짝지어지면 "빨강=상승·파랑=하락"인 한국
 * 증시 관례를 연상시켜 실제 순위와 반대로 읽힐 위험이 있었다. 그래서 브랜드 오렌지·빨강·
 * 파랑·초록·앰버(전부 이 앱에서 브랜드/상태색으로 이미 쓰임)를 모두 배제하고 새로 구성했다:
 * `node scripts/validate_palette.js "#0D9488,#7C3AED,#DB2777,#4F46E5,#B45309,#0891B2" --mode light`
 * → 전체 6-체크 PASS(밝기 밴드/채도/CVD 분리/대비 전부 통과, 최소 인접 ΔE 13.6).
 *
 * 업체 식별(카테고리컬) 목적이므로 순위가 아니라 "화면에 처음 등장한 순서"로 고정 배정한다
 * (필터로 업체를 껐다 켜도 같은 업체는 항상 같은 색을 유지해야 하므로). 범례를 항상 함께
 * 표시하는 것으로 색만으로 식별하지 않게 보완한다(레이더 차트는 범례를 항상 켜둔다).
 */
export const COMPANY_CHART_COLORS = [
  "#0D9488", // teal
  "#7C3AED", // violet
  "#DB2777", // magenta
  "#4F46E5", // indigo
  "#B45309", // ochre/brown
  "#0891B2", // cyan
];

export function getCompanyColor(index: number): string {
  return COMPANY_CHART_COLORS[index % COMPANY_CHART_COLORS.length];
}
